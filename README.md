# CCAPDEV_MP02_1212
**MedRecs: A Medical Record Application**

Member: Alfonso Miguel G. Cruz

**PREREQUISITE SOFTWARE:**
- NodeJS
- MongoDB

1.) Run the command `npm install` to install the necessary packages for this application.


2.) To run the application locally, run `node app.js` to make the web application run at `localhost:3000`.
